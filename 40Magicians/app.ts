// Make a array of magician’s names. Pass the array to a function called show_magicians(), which prints 
// the name of each magician in the array.

let magician4:string[] = ["el carey", "jordunaz", "pareygz"];
function show_magicians(magician56: string[]) {
    magician56.forEach(magician56 => {
        console.log(magician56);
    });
}
show_magicians(magician4);