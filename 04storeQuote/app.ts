// Store the famous person’s name in a variable called famous_person. Then compose your message and store it in 
// a new variable called message.

let famous_person: String = "Prophet Muhammad (P.B.U.H)";
let message:string = `${famous_person} once said, "The strongest among you is the one who controls his anger."`;
console.log(message);
